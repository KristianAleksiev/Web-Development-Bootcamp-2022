//Changing the text of the element, in this case h1
$("h1").text("New text");

//Changing the text of all elements matching the criteria "button"
$("button").text("Don't click me");

// InnerHTML text manipulation with jQuery
$("h1").html("<strong> New text bolded </strong>");
