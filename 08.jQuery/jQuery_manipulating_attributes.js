// src=, href=,..

//Getting the value of an attribute
$("img").attr("src");

// Set the value of an attribute
$("a").attr("href", "https://google.com");

// Selecting a class attribute
$("h1").attr("class"); // Getting all of the classes applied to h1
